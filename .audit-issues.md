# Overnight Audit — Issue Log

Working file — used during overnight autonomous testing. Items get moved to PROGRESS.md / PENDING.md at the end and this file deleted.

## Format
- **[severity]** [page/component] description → status (open / fixed / deferred)

Severity: `crit` (broken / blocks demo) · `bug` (functional issue) · `polish` (UX paper-cut) · `a11y` (accessibility) · `note` (observation)

---

## Open issues

(populated during audit)

## Fixed

(populated as fixes land)

## Deferred

(non-blocking items for Phase-2 polish)

### Phase 1.1 — Home desktop (1440×900)

- **[polish]** Header — search placeholder truncates to "Search ink, tor..." even on 1440px wide. The input has fixed max-width; could expand more on desktop. Status: open.
- **[crit]** Home / Trending row — all 8 product cards show the same Dell-XPS workspace photo. Root cause: seed maps every printer product to `IMG.printer` (single URL). Need per-product photos OR a 4-6 photo pool per category type. Status: open.
- **[bug]** Home / category tiles — `Inkjet Printers` (motion-blur industrial printer) is dramatic but not a typical home/office inkjet. `Premium Paper` and `All-in-One Printers` are also somewhat off-theme. Status: open (will swap from verified pool).

## Fixed (round 1)

- **[crit]** Trending + PLP product image monotony → IMG.* now arrays + hash-pick. Each product picks one of 3-7 themed photos by slug hash; same product always picks same photo (stable, no flicker). Variety visible on home Trending row.
- **[bug]** AIO Printers tile = Polaroid camera → swapped to office-printer photo (white HP printer on desk).
- **[bug]** Refill Kits tile = art supplies → swapped to small-bottle photo.
- **[bug]** Printer Ribbons tile = "Take a picture" Polaroid box → swapped to another printer-cartridge photo.
- **[polish]** Header search bar placeholder truncated → reduced nav from 6→5 items, tighter padding, search container `min-w-[260px]`. Full placeholder "Search ink, toner, cartridges, printers..." now visible.

## Open issues

- **[bug]** One photo in `printer` pool depicts a cat next to a gold-tone device (unexpected, likely an Unsplash search false positive). Should identify and remove. Status: open.
- **[bug]** Some PLP product cards may have empty space at bottom (next/image aspect ratio mismatch?). Need to re-check PLP.

### Phase 1.4 — Search

- **[bug]** Typo "carttridge" returns 0 results. pg_trgm fuzzy match isn't catching single-character typos. Decisions log notes "Sprint 1 search = pg_trgm only", with tsvector + similarity threshold tuning deferred. Status: deferred (Phase-2 search polish).
- **[polish]** No-results empty state mentions "browse the categories" in plain text but doesn't link anywhere. Should be a clickable CTA. Status: open.
- ✅ Happy path "hp" returns 27 products with photo variety across cards. Good.
- ✅ Header refreshes correctly between navigations.

### Phase 1.5 — Cart

- **[crit] FIXED** Stale items in cart (OnePlus 12 from old electronics seed). Root cause: cart-load query didn't filter on `Product.deletedAt: null` / `Product.status: 'ACTIVE'`. Fix: filter added to both `getCartView` and `getCartItemCount` (header badge). Status: fixed.
- ✅ Cart layout: free-shipping bar, item rows with qty stepper / save-for-later / remove, sticky order summary with subtotal / savings / shipping / GST / total / "Proceed to checkout" button.
- ✅ INR formatting consistent throughout.

### Phase 3 — Admin

- **[polish]** Dashboard KPIs count ALL rows (including inactive/archived). Shows Categories 24 + Brands 22 even though only 18 of each are active. Counts should likely be "active" to match what customers see — or rename label to "All categories (incl. archived)". Status: open.
- ✅ Sidebar navigation present and complete.
- ✅ Sidebar info card explains "Read-only views in Sprint 1. Full CRUD lands in Sprint 1 polish..."
